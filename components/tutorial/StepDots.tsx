'use client';

/**
 * Legacy module path. Tutorial flows use {@link TutorialPhaseBar} instead.
 * File retained so Tailwind/Postcss incremental watchers do not stat a missing path after refactors.
 */
export function StepDots(_props: { active: number }) {
  return null;
}
